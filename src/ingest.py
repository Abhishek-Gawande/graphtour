"""Batch 1: ingest a target repo (code + commits + PRs) into the Cognee graph.

Walks a repository, extracts files / modules / functions and their relationships
(imports, defined-in, modified-by, owned-by), and feeds them to cognee.remember()
so the graph is built. Stub for now — implemented in Batch 1.
"""

# TODO(Batch 1): clone/point at topoteretes/cognee, extract graph, remember().
